# homebrew-tap

Homebrew tap for [inkitori](https://github.com/inkitori)'s projects.

```sh
brew tap inkitori/tap
brew install --cask --no-quarantine soundcloud-desktop
```

Casks here are updated automatically by each project's release workflow.
